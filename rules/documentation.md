> NOTE: このファイルは原則として人間が運用する。例外的に許可があった場合のみClaude Codeが修正しても良い。

# ドキュメントの書き方

## 概要

Key and Notes 各プロジェクト共通のドキュメント規約。MDドキュメントおよびコードコメントを書くときに従う。

ドキュメントを作成する、コードコメントを付記する目的は why を残すことである。
コードはどのように動くかは、実装を LLM に読ませれば簡単に導出することができるが、判断の理由は実装から読み取れないので、ドキュメントやコメントとして残す価値がある。

ただし手順書は例外である。手順書の目的は why ではなく、人間が行う作業に再現性を持たせることにある。判断の理由を書き添えたほうがよい箇所があれば、why を補足してよい。

適用範囲：

- **記録系ドキュメントを除く全てのMDドキュメント**：すべてのルールを適用する。
- **全てのコードコメント**：「語の選び方」「LLM っぽい表現の禁止」「冗長の排除」を適用する（コメントの構成自体は `coding.md` のコーディング方針に従う）。
- **記録系ドキュメント（postmortem など）**：「整形」「語の選び方」「LLM っぽい表現の禁止」「冗長の排除」を適用する（章立ては時系列など、その文書固有の型に従ってよい）。

参考：
[日本語技術文書の文章規範](https://gist.github.com/k16shikano/fd287c3133457c4fd8f5601d34aa817d)の原文の一部を借りた。

## 整形

- コード、差分、ログ、設定ファイルの断片はコードブロックで示す。
- 定義や分類の列挙は箇条書きで示してよい。定義される用語は太字にする。
- 用語を本文中で初めて定義・導入するときも、その語は太字にする。すでに導入した語を話題として指すとき、引用、通称には「」を使い、太字と使い分ける（初出の定義は太字、以後の言及は「」）。
- ダッシュ（em ダッシュ `—`、horizontal bar `―`、いわゆる2倍ダッシュ「——」）を日本語の地の文・見出しで使わない。同格・補足の挿入（「A——挿入——B」）は括弧（）に、言い換え・敷衍（「A——B」）は句点で二文に分けるか読点でつなぐ。範囲を示す en ダッシュ `–` や英語の複合語（`Curry–Howard` など）、コードブロック、表のセルで「該当なし」を示すプレースホルダの `—` は対象外。
- 見出しに、区切り線（罫線 `─` U+2500 やダッシュ類）で「種別──主題」「主題──概念」のように二要素を詰め込まない。見出しは単一の自然な句にする（要素を一つに絞るか、助詞・読点でつなぐ）。
- 用語とその定義を並べる箇条書きは、区切り線ではなくコロンで「**用語**：説明」と書く。全角・半角のどちらでもよい。

## 語の選び方

- 対象を指す語は具体的に選ぶ。「AI」「ツール」のような広い語でぼかさない。
- 章や節で定式化・術語（契約、不変条件など）を導入したら、以後はその語で通す。「文脈」「ツール」「AI」のような曖昧語に後退しない（定式化する前の導入語として「文脈」などを使うのはよい）。
- 術語・訳語は、その分野で慣用されている語を選ぶ（プッシュ通知は「配送」ではなく「配信」、など）。意味の近い漢語を一般語の感覚で充てない。
- 術語の響きを持つ語を、術語でない場面に流用しない（システムから人間までの連なりを「経路」と呼ぶ、など）。「届くまでの流れ」「あいだに何があるか」のように普通の言い方で書く。
- 英字の一般語を日本語の地の文にそのまま混ぜない。動詞・名詞として使う語は日本語で書く（「fix する」ではなく「修正する」、「performance が悪い」ではなく「性能が悪い」）。固有名詞、製品・サービスが定める資源名・機能名、コマンド名・変数名・型名などコード上の識別子、慣用的に定着した略語（HTTP、CPU など）はそのまま使ってよい。
- 直訳調の造語や比喩を持ち込まない。英語の語を漢語・カタカナに1対1で置き換えず、意味を日常語に開く。同じ意味なら熟語を重ねるより訓読みの和語を選ぶ（「差異が存在する」ではなく「違いがある」、「〜を実施する」ではなく「〜する」）。ただし固有名と定着した術語は無理に訳さない。

## LLM っぽい表現の禁止

LLM が大量生成する、中身のない型に誘惑されない。書き上げたら、この節で点検する。

次のような言い回しは、論点を増やさず「ちゃんと書いている感」だけを付ける LLM 口調である。使わない。

- **予告と総括**：「重要なのは〜である」「本章では〜を扱う／探求する」「ここでは〜について見ていく」「まとめると」「要するに」（直前の言い換えだけのとき）、「〜に他ならない」
- **正面から系**：「正面から扱う」「正面から回収する」「正面から見る／書く／立てる」（中身の代わりに姿勢だけを宣言する）
- **空虚な形容**：「不可欠」「核心的」「鍵となる」「根本的な」（主張の中身を説明せず強調だけする）、「多角的」「包括的」「総合的」（何をどう見たかを書かない）
- **空虚な動詞**：「掘り下げる」「深掘りする」「言語化する」（何をどう書いたかを示さず終わる）、「触れる」「言及する」（一段落で済ませるだけ）
- **接続の型**：「〜において」「〜という側面から」「〜の観点から」（新情報なし）、「さらに」「また」「加えて」の連打
- **弱い緩和と称賛**：「〜と言えるだろう」「〜かもしれない」（根拠なく主張を弱める場合だけ。推量・仮定・読者の疑念なら残す）、「非常に」「極めて」「大いに」（中身のない強調）

悪い例：「本章では、〇〇の理論を正面から扱う」「この前提を、ここで正面から回収する」「多角的に分析すると、重要なのは〜である」。
良い例：「本章では、〇〇の理論を扱う」「ここで、この前提を回収する」「評価の核心は、正しさを誰が知っているかにある」。

## 冗長の排除

無駄な文章をなるべく残さない。

- 同じ主張を言い換えて繰り返さない。一つの主張は一度だけ書く。
- 隣接する節が同じことを別の角度で述べているなら、役割が重複している。片方に吸収して一つの節にまとめる。
- 場面を描写した直後に、その内容を要約し直さない。意味づけの一文（「このような作業は、ほぼ完全に任せられる」など）だけを置く。
- 同じ論理的役割を持つ並列の事実は、文を分けて重ねず一文にまとめる。その事実群の論理的地位は文頭の語で示す（「当然、経理部の月次処理も顧客の支払いも〜」）。
- 読者が自力で補える中間段階の説明は書かない。
- 数文にわたる議論を一文に圧縮できるなら、圧縮した一文だけを残す。要約の合図として「要するに」を使ってよい。
- 接続や評価のためだけの文（「それ自体はよいことである」など）を置かない。
- 想像上の読者との問答（問いを立てて一語で答える形など）を修辞として使わない。主張はそのまま述べる。読者の反応を演じて応答する形（「〜と感じたかもしれない。そのとおりである」）も同様に避け、譲歩は地の文で簡潔に行う。
- 文脈を最短で読み手と共有できる文章にする。導出を一歩ずつ展開しなくても伝わるなら、構造に名前を与えて言い切る。
- 本文でまだ導入していない概念や文書名を、先回りして持ち出さない。
- ためらいのある弱い述語（「有効な対策であり」など）で済ませない。本文内の根拠で確定していることは強く具体的に言い切る（「活用において必須であり」など）。ただし、不確実性・可能性・仮定・読者の疑念を表すための弱い述語は保持する。

## 見出しの付け方

見出しは内容を特定できる具体的なものにする。その節が答える問い、または扱う対象を指す句にする。

- 作業の手順だけを述べる見出し（「例に戻す」「〜を読み直す」など）や、情報量のない見出しにしない。その節が答える問い、または扱う対象を見出しにする。
- 節で扱っている対象を指す名詞句でもよい。
- 見出しが疑問形か断定形かは問わない。問うのは、扱う対象や読者の持つ問いを指しているかである。
- 疑問形か、対象を指す名詞句かは、本文のトーンに合うほうを選ぶ。
- 章や節に番号を打たない（「2. 背景」「2.1 制約」のような見出しにしない）。番号で参照された箇所は、章節の追加・削除・並べ替えのたびに全参照を追従させる必要があり、保守が難しくなるため。ただし、番号自体が識別子・法的慣用・実行順序など内容そのものを表す文書では番号を付けてよい（手順書の実行順序、契約書の「第N条」、外部 SSoT に対応する ID、本文から参照される選択肢ラベルなど）。

## ADR の構成

- **ステータス**：Proposed / Accepted / Superseded by ADR-xxx。後続の ADR に置き換えられたら Superseded にし、後続へリンクする。
- **結論**：設計変更の目的・概要と、それによって得られるメリットを簡潔に記述する。
- **背景・課題**：なぜこの判断をするに至ったかがわかるように。案を比較するときに考慮した制約もここに含める。
- **不採用案**：検討したが不採用にした案があれば、不採用の理由とともに書く。なければ書かない。ユーザーが判断していないことは書かない。

注意事項：

- 結論と背景・課題を読めば why がわかるように書く。不採用案を含め、各項目は要点に絞って簡潔に書く。
- 採用した設計の詳細はコードや設計文書に委ね、ADR には書かない。
- 決定後に内容を変更・補足するときは、本文を書き換えず「Amendment: 日付と表題」の節を末尾に追記する。

## BDR の構成

BDR (Business Decision Record) は、技術的なトレードオフではなく仕様・業務ルールとして下した決定を記録する。設計・実装上の選択肢を比較して決めたことは ADR に、規約・法務・収益方針・プラットフォームポリシーなど仕様寄りの判断は BDR に書く。

- **ステータス**：Proposed / Accepted / Superseded by BDR-xxx。後続の BDR に置き換えられたら Superseded にし、後続へリンクする。
- **結論**：採用する仕様や業務ルールを簡潔に記述する。
- **背景・課題**：このルールが必要になった業務上の背景（法務・規約要件、収益方針、プラットフォームの制約、ユーザーからの要望など）。検討時に考慮すべき制約もここに含める。
- **詳細**：ルールの具体的内容（条件分岐、計算式、対象外条件など）を要点に絞って書く。結論で完結するなら省略する。

注意事項：

- 結論と背景・課題を読めば why がわかるように、簡潔に書く。
- 決定後に内容を変更・補足するときは、本文を書き換えず「Amendment: 日付と表題」の節を末尾に追記する。
