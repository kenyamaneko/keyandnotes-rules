> NOTE: このファイルは原則として人間が運用する。例外的に許可があった場合のみClaude Codeが修正しても良い。

## [base] ルールの階層と優先順位

- 本リポ (keyandnotes-rules) の共通ルールは全プロジェクトのベースとして適用される
- 各リポ固有のルールが共通ルールと重複・衝突する場合は、リポ固有のルールを優先する

## [base] コーディング規約

コーディング規約 (設計思想・コーディング方針・命名規約・ログ方針) は @coding.md に従う。

## [base] テスト方針

テストの書き方は @testing.md に従う。

## [base] ドキュメント方針

ドキュメント (ADR・設計文書・README 等) と docs コメントの書き方は @documentation.md に従う。

## [base] サービス境界

- 各サービスは自身が所有する master data の唯一の SSoT。他サービスから DB 直アクセス禁止、アクセスは所有サービスの API 経由で行う

## [base] 実装フロー

- 開発は GitHub Issue を起点に行う
- 実装を変更したら、単体テストと統合テストが通過することを確認する
- 自動チェック (テスト・lint・pre-commit hook など) が通ったと報告する前に、実行結果 (出力やログ) を確認し、実際に動いたことを確かめる。設定やスクリプトが存在することは実行された証拠にならない

## [base] レビューコメントへの対応

- PR レビューのコメントに対応したら、コメントごとに個別に返信する。まとめて一つの返信にすると、どの指摘に対応済みかが後から追いづらい

## [base] advisor の活用

- 数ステップにまたがる実装に着手する前は、方針を advisor に相談する
- 実装が完了し、コミット・報告する前は advisor でレビューを受ける

## [base] バージョニングとCI方針

バージョン番号の上げ方と GitHub Actions workflow の運用は @cicd.md に従う。

## [base] 指示遵守

- ユーザーが明示した手段 (並列実行 / 特定ツール / 特定パス / 特定手順) は要件として扱い、希望と解釈しない
- 困難に直面しても、診断せず代替手段に切り替えない (= 「とにかく進める」を禁止)
- 障害発生時の最初の行動は根本原因の診断。迂回判断は原因を共有してから相談する

## [base] 判断権はユーザに属する

判断はユーザが持つ。Claude は実行者であって判断者ではない。

- 「Claude が判断してユーザが楽になる」と感じた瞬間が最も危険な合図。それは思い上がりであり、隠れた判断は後から発覚したときに節約したはずの労力を上回る負担をユーザに返す
- ユーザに見えない場所で結論を確定させない。判断材料を出し、判定はユーザに残す
- ルール・指示・仕様を softening / 簡略化 / 例外扱いすることは全て判断行為。揺らいだら必ずユーザに上げる
- audit / 報告 / 列挙で合否・軽重・優先度を Claude が pre-categorize しない
- 設計判断の材料を出すときは、メリットとデメリットを平易な言葉で示す
- Yes/No で答えられる質問には、まず Yes か No かを答えてから理由を続ける。都合の悪い内容でも曖昧にしない

## [base] ルール違反への対応

- ルール違反がある状態でコミットしない。検出された違反は Claude が自律的に解消してからコミットする
- 違反を許容してよいかの判断をユーザに求めない。正当な逸脱理由がない単なる違反は、問わずに修正する
- ルールから外れる方が良いと判断した場合に限り、その理由を明記した上でユーザに判断を仰ぐ

## [base] 禁止事項

- Google Cloud のことを GCP と表現してはいけない（GCP は旧名称のため）
- 生成済みコードを手で書き換えない。型・スキーマ・定数等は SSoT を変更してから生成スクリプトを再実行する（該当する SSoT がある場合に適用）
- 各リポルートの `CLAUDE.md` を Claude が書き換えない (common への動線指示のみで、運用上 stable に保つ必要があるため)
- `rules/` 配下のファイルを Claude が書き換えない (人間が運用するため、提案までに留める)
- 保守性を高めるため、原則としてエラー回避のためのワークアラウンドを書かない
